#include<bits/stdc++.h>
#define ull unsigned long long int
#define ll long long int
#define pc(k, a) printf("Case %d: %d\n", ++k, a)
using namespace std;

int main()
{
    string s, ns;
    cin>>s;
    int c = 1;
    ns = s;
    int len = s.size();
    int l = ns.size();
    for(int i = 0;i<len;i++)
    {

    }
}
