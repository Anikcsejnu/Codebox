#include<bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    int n, t, k, d, co = 0, cn = 0;

    scanf("%d%d%d%d", &n, &t, &k, &d);
    co = n/k;
    if(n%k!=0)
    {
        co++;
    }
    co = co * t;

    while(n!=0)
    {

    }

    return 0;
}


